<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>disable</Nullable>
    <RootNamespace>$(MSBuildProjectName.Replace(".NetStandard", "").Replace(" ", "_"))</RootNamespace>
    <AssemblyName>$(MSBuildProjectName.Replace(".NetStandard", ""))</AssemblyName>
  </PropertyGroup>

</Project>
